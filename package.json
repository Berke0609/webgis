{
  "name": "web-gis-project",
  "version": "1.0.0",
  "description": "PERN project deployed on Render.com",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client": "cd client && npm start",
    "server": "nodemon server.js",
    "build": "cd server && npm install  && cd ../client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.9.0"
  },
  "engines": {
    "node": ">=20.2.0",
    "npm": ">=6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "your-git-repo-url"
  }
}
